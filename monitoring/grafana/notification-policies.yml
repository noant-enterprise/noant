apiVersion: 1
policies:
  - orgId: 1
    receiver: noant-ops
    group_by: ['alertname', 'severity']
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
    routes:
      - receiver: noant-ops
        group_by: ['alertname']
        continue: false
